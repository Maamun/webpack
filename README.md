A laracast git tutorial
# Examples
```````
git status / git add / git remote / git clone / git commit / git push
 git fetch = git fetch && git merge
 git pull == pulls in the changes from the remote repo that other developers have added it
 // it is a shorcut for two other commands : git fetch and git merge
